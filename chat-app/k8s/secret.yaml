# Chat-App Secrets — DO NOT commit actual values
# Create the secret manually on the cluster:
#
# kubectl create secret generic chat-app-secrets -n chat-app \
#   --from-literal=NEXT_PUBLIC_SUPABASE_URL=https://supabase.noahsark.me \
#   --from-literal=NEXT_PUBLIC_SUPABASE_ANON_KEY=<anon-key> \
#   --from-literal=SUPABASE_SERVICE_ROLE_KEY=<service-role-key> \
#   --from-literal=NOTIFY_URL=http://notify.notify.svc.cluster.local:4100 \
#   --from-literal=NOTIFY_API_KEY=<notify-api-key>
#
# Also create the GitLab registry pull secret:
#
# kubectl create secret docker-registry gitlab-registry -n chat-app \
#   --docker-server=gitlab.noahsark.me:5050 \
#   --docker-username=cockpit-bot \
#   --docker-password=<gitlab-pat>
